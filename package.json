{
  "name": "synax-ai",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "monoup build",
    "dev": "bun run --filter '*' dev",
    "lint": "bun run --filter '*' lint",
    "clean": "monoup clean",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\""
  },
  "devDependencies": {
    "bun-types": "^1.3.9",
    "monoup": "^0.4.0",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3"
  }
}
