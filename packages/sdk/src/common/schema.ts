/**
 * Schema types are re-exported from @nerax-ai/plugin for consistency.
 * This ensures a single source of truth for schema definitions across all packages.
 */
export type {
  Schema,
  SchemaField,
  StringField,
  NumberField,
  BooleanField,
  SchemaEnumOption,
} from '@nerax-ai/plugin';
